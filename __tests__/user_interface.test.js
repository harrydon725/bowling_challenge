const Scorecard = require("../scorecard");
const user_interface = require("../user_interface");

describe('User_interface',()=>{
    it('Checks userinterface keeps track of the rounds',()=>{



    })
})